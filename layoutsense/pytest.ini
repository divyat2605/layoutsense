[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests that require PaddleOCR model weights (deselect with -m "not integration")
addopts = -v --tb=short -m "not integration"
